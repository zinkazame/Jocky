# Agent Overview

The proposed agent would receive an authorized task, invoke a supported
collection adapter, attach metadata, and return a result to the controller.
The repository contains transport and native collection experiments, but no
single complete agent executable or stable task contract was found.

Status: PARTIALLY IMPLEMENTED. Treat the runtime as an integration target, not
as a deployable product.